// Placeholder route for the initial scaffold (issue #1). The real Setup and
// Convert screens land in issues #14 and #15, once the design system (#2),
// codec model (#3), and file intake (#9) exist for them to build on.
function App() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-sm text-gray-500">Audio Converter — web app scaffold</p>
    </main>
  )
}

export default App
